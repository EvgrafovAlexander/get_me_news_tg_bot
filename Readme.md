# RSS Telegram Bot

Автоматический бот для Telegram, 
который каждые N минут проверяет список RSS-источников,
фильтрует только новые статьи, исходя из списка ранее учтённых, и рассылает сообщения подписчикам.
Подписчики могут подписываться и отписываться через команды */start* и */stop*.

### Возможности:

- 🔄 Периодическая проверка RSS-лент (по умолчанию каждые 20 минут)
- 📬 Автоматическая рассылка свежих статей подписчикам
- 👥 Управление подпиской через команды:
  - /start — подписаться на рассылку
  - /stop — отписаться от рассылки
- 🧠 Учёт ранее отправленных статей и игнорирование их при дальнейшей отправке
- 📅 Учитываются только публикации не старше LAST_DAYS_ARTICLES дней (по умолчанию - 3)
- 🗄️ Хранение данных об отправленных статьях и действующих подписчиках в SQLite
- 🐳 Запуск в контейнере посредством docker-compose
- 🔧 Настройки через .env

### ENV-переменные для управления ботом:

| Переменная              | Обязательная | Описание                                                                                                       | Пример             |
| ----------------------- | ------------ |----------------------------------------------------------------------------------------------------------------| ------------------ |
| **BOT_TOKEN**           | ✔️           | Токен Telegram-бота, полученный от @BotFather                                                                  | `123456:ABCDEF...` |
| **RSS_REFRESH_MINUTES** | ❌            | Интервал между проверками RSS-лент (в минутах). По умолчанию — 20.                                             | `20`               |
| **LAST_DAYS_ARTICLES**  | ❌            | Максимальный возраст статьи (в днях) при запросе. Статьи старше этого значения игнорируются. По умолчанию - 3. | `1`                |
| **MANUAL_RUN**          | ❌            | Если указано `True`, бот запускается однократно в ручном режиме.             | `True`             |


### Для запуска приложения в контейнере склонировать репозиторий и последовательно выполнить:

*docker compose build --no-cache*

*docker compose up -d*
